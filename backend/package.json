{
  "name": "backend",
  "version": "1.0.0",
  "description": "backend for csc436 react app. will start off as mockAPI then go to actual express in separate folder probably (based on class stuff)",
  "main": "index.js",
  "scripts": {
    "start": "json-server-auth db.json --port 4000"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "json-server": "^0.17.0",
    "json-server-auth": "^2.1.0"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
